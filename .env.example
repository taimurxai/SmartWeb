# Database connection string (PostgreSQL for Vercel)
DATABASE_URL="postgresql://user:password@localhost:5432/smartweb?schema=public"

# Secret key for JWT Token generation (Session management)
JWT_SECRET="generate_a_random_secret_key_here_for_security"
